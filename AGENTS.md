# AGENT.md

디유 프로젝트에서 AI 코딩 에이전트가 정확하고 효율적으로 작업하기 위한 지침이다.
이 문서는 프로젝트 설명서가 아니라, 코드 변경 시 따라야 할 작업 원칙을 정의한다.

## 1. 기본 방향

디유 프로젝트는 기본적으로 전통적인 Spring Boot MVC 계층형 구조를 따른다.

- Controller는 HTTP 요청과 응답을 담당한다.
- Service는 비즈니스 로직과 트랜잭션 흐름을 담당한다.
- Repository는 데이터 접근을 담당한다.
- Entity는 데이터베이스 테이블과 매핑되는 객체를 담당한다.
- DTO는 계층 간 입력과 출력 데이터를 전달한다.

특별한 요청이 없는 한 복잡한 패턴, 과한 추상화, DDD 스타일 분리, 헥사고날 아키텍처를 임의로 도입하지 않는다.

## 2. 코딩 전 확인

구현 전에 요구사항과 작업 범위를 먼저 확인한다.

- 요구사항이 명확하면 바로 진행한다.
- 이해되지 않는 부분이 있으면 임의로 해석하지 않는다.
- 여러 해석이 가능하면 가능한 해석을 나누어 설명한다.
- 현재 코드 흐름과 맞지 않는 요청이면 그 이유를 말한다.
- 더 단순한 구현 방법이 있으면 먼저 제안한다.

확실하지 않은 상태에서 추측으로 코드를 작성하지 않는다.

## 3. 기술 선택이 필요한 경우

기술 선택이나 설계 선택이 필요한 상황에서는 에이전트가 혼자 결정하지 않는다.

예를 들어 다음과 같은 경우에는 개발자에게 선택을 요청한다.

- 라이브러리 또는 의존성 선택
- 인증 방식 선택
- 응답 구조 선택
- 예외 처리 방식 선택
- DB 설계 방식 선택
- 파일 저장 방식 선택
- 동기 / 비동기 처리 방식 선택

이때 각 선택지에 대해 간단히 정리한다.

- 선택지 A: 장점 / 단점 / 적합한 상황
- 선택지 B: 장점 / 단점 / 적합한 상황
- 추천안이 있다면 추천 이유

단, 이미 프로젝트에서 선택된 방식이 있으면 기존 방식을 우선한다.

## 4. 단순하게 구현

항상 가장 작은 변경으로 문제를 해결한다.

- 요청받지 않은 기능을 추가하지 않는다.
- 한 번만 쓰이는 코드에 불필요한 추상화를 만들지 않는다.
- 미래 확장성을 이유로 과한 구조를 만들지 않는다.
- 간단한 CRUD에 복잡한 설계를 적용하지 않는다.
- 코드가 길어졌다면 더 짧고 명확한 방식으로 줄일 수 있는지 검토한다.

좋은 구현은 많은 구조를 만드는 것이 아니라, 현재 요구사항을 명확하게 해결하는 것이다.

## 5. 필요한 부분만 수정

기존 코드를 수정할 때는 요청과 직접 관련된 부분만 변경한다.

- 주변 코드를 임의로 리팩터링하지 않는다.
- 기존 스타일을 존중하고 그대로 맞춘다.
- 관련 없는 주석, 포맷, 변수명을 고치지 않는다.
- 발견한 문제라도 요청 범위 밖이면 수정하지 말고 언급만 한다.
- 내가 만든 변경으로 생긴 불필요한 import, 변수, 메서드는 정리한다.

모든 변경 라인은 사용자의 요청과 직접 연결되어 있어야 한다.

## 6. 검증 가능한 목표로 작업

작업은 검증 가능한 단위로 나누어 진행한다.

- 기능 추가: 요청 DTO, Service 로직, Repository 호출, Controller 응답까지 확인한다.
- 버그 수정: 문제가 발생하는 조건을 먼저 파악하고, 수정 후 같은 조건에서 해결됐는지 확인한다.
- 리팩터링: 동작이 바뀌지 않았는지 기존 테스트 또는 실행 흐름으로 확인한다.

가능하면 다음 순서로 작업한다.

1. 변경 목표를 정한다.
2. 필요한 파일만 수정한다.
3. 컴파일 또는 테스트로 확인한다.
4. 수정 내용과 검증 결과를 간단히 정리한다.

## 7. 작업 결과 보고

작업을 마친 뒤에는 다음만 간단히 보고한다.

- 무엇을 변경했는지
- 어떤 기준으로 구현했는지
- 검증했는지 여부
- 남은 주의사항이 있는지

장황한 설명보다 실제 변경 내용과 이유를 명확히 전달한다.
